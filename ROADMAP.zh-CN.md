# CEM-S Studio 开发路线图

## 产品目标

CEM-S Studio 的目标不是简单地把 Blockbench Cube 转换成 GLSL，而是为 CEM-S 提供完整、直观的可视化编辑环境。

资源包作者应当能够完成以下流程，而不需要理解 GLSL、顶点编号或 CEM-S 内部文件结构：

1. 新建 CEM-S Studio 项目。
2. 选择 Minecraft 版本和目标实体。
3. 自动加载对应的原版参考模型、纹理和锚点。
4. 在 Blockbench 中创建模型并将 Group 拖入目标锚点。
5. 调整位置、旋转、缩放、UV 和渲染属性。
6. 一键创建新资源包，或更新已有 CEM-S 资源包。

## 支持范围

### 计划直接支持

- 使用 Minecraft `entity` shader 渲染的大部分生物和实体。
- 使用 Armor / Equipment 渲染路径的盔甲、鞘翅和类似附件。
- 玩家、人形生物、四足生物、特殊结构生物、投射物、载具等常见实体类别。
- Minecraft `1.21.6`、`1.21.11` 和 `26.1+`，并为不同版本维护独立实体数据。

### 需要扩展 CEM-S runtime 才能支持

- 使用独立 shader 的方块实体。
- 物品、粒子和不经过 CEM-S 当前 Entity / Armor 挂钩的渲染对象。
- 原版 shader 无法提供稳定识别信息的特殊渲染层。

这些对象不能仅通过增加 Studio 预设实现，需要先扩展 CEM-S shader runtime。

## 核心设计原则

- 默认流程只展示资源包作者需要理解的概念。
- 将 GLSL、顶点编号和纹理检测参数放入专家模式。
- 拖入锚点即视为绑定，不要求用户额外执行绑定命令。
- 参考模型只用于定位，不参与贴图生成和模型导出。
- 常见实体由数据驱动，不在插件界面代码中逐个硬编码。
- `.cemst` 保存完成工作所需的全部项目元数据。
- 生成资源包时保留不属于 CEM-S Studio 管理范围的用户文件。

## 阶段一：实体数据库与新建向导

建立按 Minecraft 版本区分的实体数据库。每条实体记录至少包含：

- 实体 ID 和显示名称。
- 实体类别和搜索关键词。
- Entity 或 Armor / Equipment 渲染目标。
- 原版参考模型及其部件层级。
- 默认纹理尺寸和参考纹理。
- 可绑定锚点及其原版部件对应关系。
- CEM-S 纹理标记位置和颜色。
- 面数量、锚点面编号、轴方向和 CEM 区域尺寸。
- 不同变种或渲染层的兼容信息。

新建项目时提供可搜索的实体选择器。选择实体后自动完成：

- 创建项目设置。
- 添加参考模型。
- 注册锚点。
- 选择正确的检测预设。
- 设置纹理尺寸和资源包输出路径。

### 首批实体

- 玩家、盔甲架。
- 僵尸、骷髅、村民及常见人形生物。
- 猪、牛、羊、狼、猫、马和常见四足生物。
- 箭、船、矿车。
- 鞘翅和常见 Armor / Equipment 目标。

### 验收条件

- 用户新建项目后不会进入空白且无提示的界面。
- 常见实体不需要手动填写检测参数。
- 实体数据可以在不修改 UI 代码的情况下增加。
- 不同 Minecraft 版本可以为同一实体提供不同数据。

## 阶段二：自动绑定与锚点工作流

- 将用户 Group 或 Cube 拖入参考锚点后自动建立绑定。
- 从锚点移出时自动解除或更新绑定。
- 在 Outliner 中区分参考部件、用户模型和已绑定附件。
- 显示当前绑定目标，但不要求用户理解额外的绑定工具。
- 支持一个锚点挂载多个附件。
- 支持一个完整模型按不同部件分别绑定多个锚点。
- 自动计算参考模型和宿主模型之间的位置、旋转及缩放偏移。
- 保存并恢复绑定关系，重命名 Group 后不丢失绑定。

### 验收条件

- 正常工作流不需要点击“绑定”按钮。
- 拖动 Outliner 层级后，导出结果立即反映新的绑定关系。
- 参考模型不会被导出，也不会污染用户贴图。

## 阶段三：完整几何与变换支持

覆盖 CEM-S runtime 当前能够表达的几何操作：

- `ADD_BOX` 普通立方体。
- `ADD_BOX_ROTATE` 旋转立方体。
- `ADD_SQUARE` 单个平面。
- 多轴 Cube 旋转。
- 任意层级父组旋转。
- 均匀缩放。
- 将非均匀缩放烘焙到实际几何。
- 镜像模型和镜像 UV。
- 每面独立 UV。
- UV 旋转和翻转。
- 禁用单个面。
- 正确处理旋转中心和嵌套变换。

如果 CEM-S runtime 后续实现有效的 `ADD_QUAD`，Studio 再增加对应的自由四边形或 Mesh 导出。

### 验收条件

- Blockbench 预览与游戏内模型位置、大小和方向一致。
- 支持的 Blockbench 操作不会在导出阶段突然报错。
- 不支持的几何类型会在编辑时立即给出明确提示。

## 阶段四：材质与渲染属性

- 不同 Cube 使用不同 Blockbench Texture。
- 不同 Face 选择独立纹理和 UV 区域。
- 参考模型使用独立辅助显示，不占用用户 Texture。
- 支持透明纹理。
- 支持 CEM-S `EMISSIVE` 发光模式。
- 支持 `PER_FACE_LIGHTING` 逐面光照。
- 支持顶点颜色和颜色乘算。
- 支持隐藏原模型、保留原模型或只替换指定部件。
- 支持按 Group 设置渲染属性并向子节点继承。

需要确认 CEM-S 单次渲染所使用的 `Sampler0` 限制。Blockbench 中的多 Texture 必须在导出时映射到实际的 Minecraft 纹理来源或合并图集，不能只保存编辑器预览状态。

### 验收条件

- 用户模型与参考模型可以同时正确预览。
- 创建纹理时不会包含参考模型。
- Studio 能明确提示多个 Texture 将如何进入游戏资源包。
- 发光和光照选项在支持的 Minecraft 版本中生成正确 shader 配置。

## 阶段五：检测规则与原模型控制

- 将常见实体检测规则完全隐藏在实体数据库中。
- 支持纹理标记像素和颜色检测。
- 支持顶点编号、面数量和锚点面检测。
- 支持 Entity 与 Armor / Equipment 检测路径。
- 支持实体变种、不同纹理和不同渲染层。
- 支持是否隐藏未匹配原版面的控制。
- 提供检测规则预览和诊断结果。
- 专家模式允许覆盖生成的检测参数。

### 验收条件

- 普通用户不需要看到 GLSL 或顶点编号。
- 导出前可以检查目标实体是否拥有完整且有效的检测数据。
- 缺少实体数据时给出可执行的修复建议，而不是只显示 shader 错误。

## 阶段六：多模型资源包项目

- 一个 `.cemst` 工作区管理多个模型。
- 自动分配并检查模型 ID 冲突。
- 同一资源包同时包含多个实体和多个附件。
- 显示模型、实体、模型 ID 和输出文件之间的对应关系。
- 新建和更新资源包使用相同的导出检查流程。
- 更新已有资源包时保留用户自定义内容。
- 提供资源包级错误列表和重新导出状态。

### 验收条件

- 制作者不需要为每个实体单独维护一个孤立项目。
- 模型 ID 冲突在写入文件前被阻止。
- 重复导出不会产生重复 import 或破坏用户文件。

## 阶段七：专家模式与扩展能力

- 可选的原始 GLSL 片段或自定义生成钩子。
- 自定义实体数据导入和导出。
- 社区实体预设包。
- 实体数据库校验工具。
- 游戏内 shader 编译日志诊断。
- 从资源包反向读取 CEM-S Studio 管理的模型。

专家模式是逃生口，不应成为普通用户完成常见任务的前置条件。

## 当前缺口

当前版本已经具备：

- `.cemst` 项目格式。
- `ADD_BOX` 和 `ADD_BOX_ROTATE` 导出。
- 多轴旋转和父组旋转烘焙。
- 每面 UV 基础导出。
- 玩家参考模型和自定义参考模型导入。
- 基础锚点绑定。
- Entity / Armor 输出路径。
- 资源包创建和更新。
- Minecraft 多版本 runtime。

当前主要缺口是：

- 实体预设数量过少且仍由界面硬编码。
- 绑定尚未随着 Outliner 变化实时同步。
- `ADD_SQUARE`、UV 旋转、禁用面和非均匀缩放尚未导出。
- 多 Texture 尚未完成 Minecraft 运行时映射策略。
- 发光、逐面光照、颜色和原模型显示控制没有可视化属性面板。
- 一个项目只能管理单个主要模型。

## 推荐实施顺序

1. 定义实体数据库 schema，并完成玩家、猪和鞘翅三类数据样例。
2. 将新建向导改为实体搜索选择器，并自动创建参考模型。
3. 实现 Outliner 拖动后的实时自动绑定。
4. 明确多 Texture 的图集或纹理来源映射方案。
5. 补齐平面、UV、缩放和面控制。
6. 增加 Group / Cube 的 CEM-S 渲染属性面板。
7. 扩展首批实体数据库并建立版本回归测试。
8. 实现多模型资源包工作区。
9. 最后增加专家模式和 runtime 扩展能力。

## 发布要求

每个阶段发布前至少完成：

- Node 单元测试。
- 当前最新版 Blockbench 的干净配置 smoke 测试。
- Minecraft 对应版本的资源包加载和 shader 编译日志检查。
- 至少一个玩家附件、一个普通生物附件和一个特殊实体的游戏内验证。
- `.cemst` 向前迁移测试。
- 确认参考模型和测试文件没有进入最终资源包。
